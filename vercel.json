{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "buildCommand": "pnpm turbo run build --filter=@legalize-pe/web --force && mkdir -p .vercel && cp -r apps/web/.vercel/output .vercel/output",
  "installCommand": "pnpm install",
  "framework": null
}
